#include "Processor.h"

Processor::
Processor() : Unit("Processor") {
  // aluOne = ALU(1);
  // aluTwo = ALU(2);
  // aluThree = ALU(3);


  // muxRegDist = Multiplexor(1);
  // muxJump = Multiplexor(2);
  // muxBranch = Multiplexor(3);
  // muxMemToReg = Multiplexor(4);
  // muxALUSrc = Multiplexor(5);

  // std::string sillyString = "Pass first instruction address here! TODO";

  // programCounter = ProgramCounter(sillyString);

}